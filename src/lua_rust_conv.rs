//! As the name suggests, conversion between Lua and Rust
//! data structures.

use super::*;
use mlua::{Lua, Result, Function, Table, Value};

pub impl Table {
    fn toRust(ltable: &Table) ->  {
    }
}
